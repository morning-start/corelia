# 性能分析

> Corelia 性能瓶颈与优化建议

---

## 性能目标

| 指标 | 目标值 | 当前状态 |
|------|--------|----------|
| 冷启动时间 | < 2s | ✅ 达标 |
| 窗口唤起延迟 | < 100ms | ✅ 达标 |
| 搜索响应时间 | < 50ms | ⚠️ 需优化 |
| 内存占用（空载） | < 100MB | ✅ 达标 |
| QuickJS 插件加载 | < 10ms | ✅ 达标 |
| Webview 插件加载 | < 500ms | ✅ 达标 |

---

## 高优先级问题

### 问题1：VM 双重缓存

**位置**: `src/lib/plugins/service.ts` + `src-tauri/src/plugins/loader.rs`

**问题描述**: 
前端和后端各自维护 VM 缓存，可能导致状态不一致。

**影响**: 
前端可能尝试使用已被后端销毁的 VM，导致错误。

**优化方案** (MVP 适用):
- 移除前端 VM 缓存，改为纯代理模式
- 所有 VM 操作直接调用后端

**实施成本**: 4h  
**风险**: 低

---

### 问题2：搜索串行执行

**位置**: `src/lib/stores/search/index.ts`

**问题描述**: 
对匹配插件逐一搜索，串行执行。

**影响**: 
如果匹配 5 个插件，每个耗时 100ms，总耗时 500ms。

**优化方案**:
```typescript
// 改为并行执行
const results = await Promise.allSettled(
  matchedPlugins.map(p => pluginService.executeSearch(p.name, query))
);
```

**实施成本**: 2h  
**性能提升**: 显著（从 O(n) 到 O(1)）

---

### 问题3：WASM 桥接轮询模式

**位置**: `src-tauri/src/plugins/api_bridge.rs` + `wasm_bridge.rs`

**问题描述**: 
QuickJS 调用 WASM 后，通过轮询获取结果。

**影响**: 
浪费 CPU 资源。

**优化方案** (MVP 适用):
- 短期：增加指数退避策略（10ms → 20ms → 40ms，上限 100ms）
- 长期：关注 rquickjs 的 Promise 支持

**实施成本**: 2h

---

## 中优先级问题

| 问题 | 位置 | 优化方案 | MVP适用 | 成本 |
|------|------|----------|---------|------|
| fetch API 同步阻塞 | api_bridge.rs | 用 tokio 处理异步 IO | ❌ MVP后 | 8h |
| 拼音搜索冗余计算 | fuzzy.ts | 先判断是否含中文 | ✅ 是 | 1h |
| WindowService 缓存状态 | window_service.rs | 直接调用 is_visible() | ✅ 是 | 1h |
| 插件目录每次扫描 | loader.rs | 增加文件监听 | ❌ MVP后 | 4h |

---

## 性能优化技巧

### 前端优化

1. **使用 Svelte 5 Runes** - 更精细的响应式控制
2. **组件懒加载** - 非关键组件按需加载
3. **避免不必要的响应式** - 不需要响应的状态不用 `$state`
4. **搜索防抖** - 快速输入时减少搜索次数

### 后端优化

1. **避免阻塞操作** - 文件 IO 等耗时操作用 async
2. **资源池化** - VM、连接等复用
3. **懒加载** - 插件仅在使用时完整加载
4. **缓存策略** - 合理使用缓存，避免双重缓存

---

## 性能监控

### 开发时监控

```bash
# 查看编译产物大小
bun run build && ls -la build/

# 查看内存占用
# Windows: 任务管理器
# 或使用 Rust 内存分析工具
```

### 关键指标

| 指标 | 测量方法 |
|------|----------|
| 冷启动时间 | 从点击图标到窗口出现 |
| 搜索响应 | 输入到结果展示 |
| 插件加载 | 首次使用插件的加载时间 |
| 内存占用 | 任务管理器查看 |

---

## 与竞品对比

| 指标 | uTools | ZTools | Corelia |
|------|--------|--------|---------|
| 冷启动 | ~1s | ~1.2s | ~0.5-1s |
| 搜索响应 | <20ms | <20ms | <20ms (优化后) |
| 插件加载 | ~300ms | ~300ms | QuickJS: ~7ms |
| 内存占用 | ~150MB | ~180MB | ~40-100MB |

---

## MVP 优化清单

| 优先级 | 优化项 | 预期收益 | 状态 |
|--------|--------|----------|------|
| P0 | 插件搜索并行化 | 性能大幅提升 | ⏳ 待实施 |
| P0 | 移除前端 VM 缓存 | 简化逻辑 | ⏳ 待实施 |
| P1 | WASM 轮询退避 | 减少 CPU 占用 | ⏳ 待实施 |
| P1 | 拼音搜索短路 | 英文搜索优化 | ⏳ 待实施 |

---

## 相关文档

- [插件系统架构](./plugin-system.md)
- [安全性分析](./security.md)
- [MVP 开发指南](../development/mvp-guide.md)
